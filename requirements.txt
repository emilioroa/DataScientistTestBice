# Dependencias del proyecto — Data Scientist Senior · Andina Crédito
# Python 3.10+
#
# Instalación:  pip install -r requirements.txt

# --- Núcleo: manipulación de datos y modelado ---
pandas>=2.0,<3.0
numpy>=1.24
scikit-learn>=1.3     # HistGradientBoosting para los diagnósticos del EDA
                      # (fuga de información y validación adversarial),
                      # métricas y calibración

# --- Visualización interactiva (EDA) ---
plotly>=5.0
nbformat>=5.0         # REQUERIDO por Plotly para renderizar figuras en el notebook
                      # (sin esto: "ValueError: Mime type rendering requires nbformat>=4.2.0")
kaleido>=0.2          # (opcional) exportar figuras Plotly a imagen estática (PNG/PDF)

# --- Entorno de notebooks ---
jupyterlab>=4.0
ipykernel>=6.0        # kernel de Python para ejecutar el notebook en VS Code / JupyterLab
nbconvert>=7.0        # ejecutar el notebook desde la terminal (make eda)

# --- Modelado (Paso 2) ---
lightgbm>=4.0         # modelo de gradient boosting; soporta restricciones de monotonía
shap>=0.44            # explicabilidad: contribución de cada variable, global e individual
